# React Development Server Configuration
# Disable WebSocket connection for hot reloading to prevent connection errors
WDS_SOCKET_PORT=0
FAST_REFRESH=false

# Alternative: Use polling instead of WebSocket for file watching
CHOKIDAR_USEPOLLING=true
